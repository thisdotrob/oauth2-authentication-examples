{
  "name": "authorization-code-example-client",
  "description": "A NodeJS client for authenticating with the 'client credentials' OAuth2 grant",
  "main": "app.js",
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "request": "^2.81.0"
  },
  "devDependencies": {
    "prettier": "^1.4.4"
  },
  "scripts": {
    "start": "node app.js",
    "style-check": "prettier '**/*.js' --list-different --single-quote --trailing-comma all",
    "style-fix": "prettier '**/*.js' --write --single-quote --trailing-comma all"
  }
}
